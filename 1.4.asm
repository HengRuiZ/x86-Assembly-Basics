DATA SEGMENT
A DB 5
B DB 0
DATA ENDS
CODE SEGMENT
ASSUME CS:CODE,DS:DATA
START:MOV AX,DATA
MOV DS,AX

MOV AL,A
MOV AH,A
MOV CX,3
MOV BX,0
SHR AL,1
JNC NEXT

AGAIN:	ADD B,AH
	JMP CTN

NEXT:	SHL AH,1
	SHR AL,1
	JC AGAIN
CTN:	DEC CX
	JNZ NEXT

MOV AX,4C00H;
INT 21H
CODE ENDS
END START